var inherit = require("./lib/inherit");
var ProcessPool = require("./lib/ProcessPool");
var Processor = require("./lib/Processor");


module.exports = {
    newFixedProcessPool : ProcessPool,
    Processor : Processor
}